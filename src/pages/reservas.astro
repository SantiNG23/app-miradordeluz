---
import ReservaCabanaPage from "../components/astro/ReservaCabanaPage.astro";

// Leer parámetro de consulta (si viene desde los botones de las cabañas)
const url = new URL(Astro.request.url);
const cabanaId = url.searchParams.get("cabana") ?? "1";
---

<ReservaCabanaPage cabanaId={cabanaId} />
